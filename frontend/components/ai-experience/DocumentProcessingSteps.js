"use client";

/**
 * Back-compat re-export — the checklist UI was replaced by a single
 * agent-style activity indicator. Prefer importing DocumentProcessingActivity.
 */
export { default } from "./DocumentProcessingActivity";
